const
    expect = require('expect'),
    {describe, test} = require('mocha'),
    assert = require('../src/assert.js');

describe('fua.core.assert', function () {

    test.only('develop', function () {
        // console.log(assert);
        // console.log(assert.number.integer);
        // assert(false, 'test');
        // assert.number.integer(10, 0, 100);
        // assert.number.integer(10, {min: 0, max: 0});
    });

});
